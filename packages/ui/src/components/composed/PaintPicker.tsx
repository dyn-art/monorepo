'use client';

import React from 'react';

import { Popover, PopoverTrigger } from '../layout';

export const PaintPicker: React.FC = () => {
	return (
		<Popover>
			<PopoverTrigger asChild>{/* TODO */}</PopoverTrigger>
		</Popover>
	);
};
